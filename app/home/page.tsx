import { title } from '@/app/components/primitives';

export default function HomePage() {
  return (
    <div>
      <h1 className={title()}>UI does not exist yet. Will be added in v2.0.0!</h1>;
    </div>
  );
}
