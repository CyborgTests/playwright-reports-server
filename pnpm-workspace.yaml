packages:
  - packages/*
  - apps/*

allowBuilds:
  better-sqlite3: true
  esbuild: true

# Respect the engines field in package.json
engineStrict: true

# Prefer the local cache when available for faster installs
preferOffline: true
